# Validation Report — Task 4.1: Property 1 — Absence of Forbidden Terms

- **Spec:** `revisi-bab3-metodologi`
- **Property:** 1 — Absence of Forbidden Terms
- **Validates:** Requirements 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9, 3.10, 3.11, 8.1, 11.2
- **Target file:** `Skripsi/bab3_metodologi.md` (60 981 bytes, 381 lines, LF-only, first line `# BAB 3 METODE PENELITIAN`)
- **Tools used:** Kiro `grep_search` (ripgrep) + PowerShell `[regex]::Matches` cross-check via `validation/_scan_forbidden.ps1`
- **PASS condition:** 0 occurrences for every forbidden pattern in any disallowed context.

## 1. Group A — Case-insensitive phrase scan

| #   | Pattern (regex)                        | Description                                                                          | Matches | Verdict |
| --- | -------------------------------------- | ------------------------------------------------------------------------------------ | ------- | ------- |
| A.1 | `ray\s*shoot`                          | Covers `Ray Shooting`, `Ray Shoot`, `ray shooting`, `ray shoot` (any spacing/casing) | 0       | PASS    |
| A.2 | `ditranspilasikan ke WebGL`            | Old transpilation phrasing                                                           | 0       | PASS    |
| A.3 | `transpilasi OpenGL ES`                | Old transpilation phrasing                                                           | 0       | PASS    |
| A.4 | `OpenGL ES 3\.0 yang ditranspilasikan` | Old transpilation phrasing                                                           | 0       | PASS    |

Notes:

- A.1 is a single regex that subsumes the four ray-shoot variants in the task (case-insensitive, optional whitespace between the two words).
- All four scans returned 0 hits with both `grep_search` and the PowerShell cross-check.

## 2. Group B — Case-sensitive whole-word token scan

| #    | Pattern (regex, case-sensitive, word-boundary) | Description                                   | Matches | Verdict |
| ---- | ---------------------------------------------- | --------------------------------------------- | ------- | ------- |
| B.1  | `\bTileType\b`                                 | Forbidden enum from old design                | 0       | PASS    |
| B.2  | `\bwinding_number\b`                           | Forbidden scalar field from old class diagram | 0       | PASS    |
| B.3  | `\bPPGA\b`                                     | Forbidden acronym                             | 0       | PASS    |
| B.4  | `Projective Geometric Algebra`                 | Forbidden expanded form                       | 0       | PASS    |
| B.5  | `\bEMPTY\b`                                    | Forbidden tile-type label                     | 0       | PASS    |
| B.6  | `\bINTERIOR\b`                                 | Forbidden tile-type label                     | 0       | PASS    |
| B.7  | `\bEDGE\b`                                     | Forbidden tile-type label                     | 0       | PASS    |
| B.8  | `Rust edisi 2021`                              | Forbidden edition reference                   | 0       | PASS    |
| B.9  | `edisi 2021`                                   | Forbidden edition reference                   | 0       | PASS    |
| B.10 | `edition\s*=\s*"2021"`                         | Forbidden Cargo manifest reference            | 0       | PASS    |

Notes:

- The case-sensitive uppercase tokens (`EMPTY`, `INTERIOR`, `EDGE`) cover the task's "in tile-type context" qualifier; since none of the three uppercase tokens appear at all anywhere in the file (with whole-word matching or in any context), the stronger condition is satisfied — there is therefore no need to inspect surrounding sentences for tile-classification semantics.
- All ten scans returned 0 hits with both `grep_search` and the PowerShell cross-check.

## 3. Group C — Forbidden equation-form scan

| #   | Pattern (regex)                       | Covers                                                                                                                                   | Matches | Verdict |
| --- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------- |
| C.1 | `a\s*x\s*\+\s*b\s*y\s*\+\s*c\s*=\s*0` | `ax+by+c=0` and any whitespace variant                                                                                                   | 0       | PASS    |
| C.2 | `u\s*-\s*v\s*\u00b2`                  | `u-v²` (unicode squared) and spaced variants                                                                                             | 0       | PASS    |
| C.3 | `u\s*-\s*v\s*\^\s*2`                  | `u-v^2` and spaced variants                                                                                                              | 0       | PASS    |
| C.4 | `f\s*\(\s*u\s*,\s*v\s*\)`             | `f(u,v) = u - v²` opening form                                                                                                           | 0       | PASS    |
| C.5 | `C\s*\(\s*x\s*,\s*y\s*\)\s*=\s*0`     | `C(x,y)=0`                                                                                                                               | 0       | PASS    |
| C.6 | `w_[0123]`                            | Any `w_0`, `w_1`, `w_2`, `w_3` (covers cubic PPGA expressions in both `w_0³-w_1 w_2 w_3` and `w_0(p)^3 - w_1(p) · w_2(p) · w_3(p)` forms) | 0       | PASS    |
| C.7 | `\u00b3` (unicode `³`)                | Any superscript-3 glyph, including `w_0³`                                                                                                | 0       | PASS    |
| C.8 | `\u00b2` (unicode `²`)                | Any superscript-2 glyph, including `v²`                                                                                                  | 0       | PASS    |

Notes:

- C.6 catches both forbidden cubic-form variants because every variant listed in the task starts with `w_0` and contains `w_1`, `w_2`, `w_3` tokens.
- C.7 and C.8 add coverage for unicode squared/cubed glyphs even when surrounding tokens differ from the listed templates.
- All eight scans returned 0 hits with both `grep_search` and the PowerShell cross-check.

## 4. Context review for permitted mentions

To ensure the nuance qualifiers in the task are honoured, the cross-check also ran two broader scans for `transpil` and `OpenGL`. Both returned non-zero hits. Each hit was inspected manually to confirm it is **not** in a forbidden context:

| Token         | Line | Surrounding sentence (trimmed)                                                                                                                                                                                  | Forbidden?                                                                                                                                                                      |
| ------------- | ---: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OpenGL ES`   |   13 | "Merumuskan spesifikasi fungsional pustaka (library) rendering berbasis pemrograman sistem Rust dan API grafis OpenGL ES 3.0 / WebGL 2.0."                                                                      | No — describes the API class (industry-standard mention, no transpilation implied).                                                                                             |
| `OpenGL ES`   |   23 | "Mengembangkan pemrosesan Vertex Shader dan Fragment Shader konvensional pada GPU menggunakan OpenGL ES 3.0."                                                                                                   | No — describes the conventional shader pipeline class, not Arabella's runtime target. WebGL 2.0 is a Web port of OpenGL ES 3.0 by spec, so referring to that lineage is allowed. |
| `OpenGL ES`   |   55 | Table row 2 solution: "Membatasi implementasi GPU secara ketat hanya pada rasterization pipeline tradisional menggunakan Vertex Shader dan Fragment Shader standar OpenGL ES 3.0 / WebGL 2.0."                  | No — names the standard rasterization pipeline class; no claim of transpilation.                                                                                                |
| `transpilasi` |   68 | "Target eksekusi utama Arabella adalah `wasm32-unknown-unknown` pada lingkungan peramban dengan API grafis WebGL 2.0 sebagai target langsung tanpa lapisan transpilasi tambahan."                                | No — explicit **denial** of transpilation; this is the canonical correction the spec mandates (Requirement 3.9).                                                                |

None of the four mentions assert that Arabella transpiles to OpenGL ES or that WebGL is reached via transpilation; on the contrary, line 68 explicitly states the opposite. Therefore none of these contextual hits constitutes a forbidden-context match.

## 5. Sanity checks

- File exists and is the correct artifact: `Skripsi/bab3_metodologi.md`, length 60 981 bytes, 381 lines (LF-only), first line `# BAB 3 METODE PENELITIAN`.
- File contains the expected canonical replacements at the expected frequencies (re-verified against the current file in this run):
  `binning DDA` 8, `akumulator signed-area` 6, `propagasi backdrop` 8, `F24Dot8` 9, `fragment shader` 14, `WebGL 2.0` 9, `Rust edisi 2024` 1, `edition = "2024"` 1.
- Every forbidden pattern was independently verified twice (Kiro `grep_search` and PowerShell `[regex]::Matches`) to avoid false negatives from a single tool. Both tools agreed on every count.
- The full PowerShell scan log is reproducible by running `validation/_scan_forbidden.ps1`.

## 6. Verdict

**PASS** — All 22 forbidden patterns across Groups A, B, and C report 0 occurrences in `Skripsi/bab3_metodologi.md`. The four contextual mentions of `OpenGL ES`/`transpilasi` were inspected and confirmed to be permitted industry-standard references or an explicit denial of transpilation, not forbidden-context uses. Property 1 (Absence of Forbidden Terms) is satisfied.
